<div class="container">
  <div class="row">
    <h2>ユーザー一覧</h2>
    <% if @records.present? %>
      <%= render 'users/user', users: @records %>
    <% else %>
      <%= render 'users/user', users: @users %>
    <% end %>
  </div>
</div>