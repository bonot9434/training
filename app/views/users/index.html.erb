<div class="container">
  <div class="row">
    <% @users.each do |user| %>
      <%= link_to user_path(user) do %>
        <%= attachment_image_tag user, :profile_image_id, :fill, 40, 40, fallback: "no_image.jpg", size:'40×40' %>
        <%= user.name %>
      <% end %>
    <% end %>
  </div>
</div>