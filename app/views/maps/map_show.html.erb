<div class="container">
  <h2 class="col-4 mt-3">ユーザー一覧</h2>
  <div class="mt-3">
    <% if @users.exists? %>
      <%= render 'users/user', users: @users %>
    <% else %>
      <div>ユーザーはいません</div>
      <% end %>
  </div>
</div>